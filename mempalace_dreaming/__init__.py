"""Pure, dependency-free dreaming engine for MemPalace memory hygiene.

This package intentionally imports no Hermes runtime. All memory reads and
writes are dependency-injected so the engine stays unit-testable.
"""
